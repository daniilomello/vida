health:
  handler: src/main/functions/health.health
  events:
    - http:
        path: api/v1/health
        method: get
        cors: ${self:custom.corsConfig}

lambdaAuthorizer:
  handler: src/main/functions/auth/authorizer.handler

createSession:
  handler: src/main/functions/auth/create-session.createSession
  events:
    - http:
        path: api/v1/auth/session
        method: post
        cors: ${self:custom.corsConfig}

refreshSession:
  handler: src/main/functions/auth/refresh-session.refreshSession
  events:
    - http:
        path: api/v1/auth/refresh
        method: post
        cors: ${self:custom.corsConfig}

deleteSession:
  handler: src/main/functions/auth/delete-session.deleteSession
  events:
    - http:
        path: api/v1/auth/session
        method: delete
        cors: ${self:custom.corsConfig}
        authorizer:
          name: lambdaAuthorizer
          resultTtlInSeconds: 300
          identitySource: method.request.header.Cookie
          type: request

createCard:
  handler: src/main/functions/cards/create-card.createCard
  events:
    - http:
        path: api/v1/cards
        method: post
        cors: ${self:custom.corsConfig}
        authorizer:
          name: lambdaAuthorizer
          resultTtlInSeconds: 300
          identitySource: method.request.header.Cookie
          type: request

getCards:
  handler: src/main/functions/cards/get-cards.getCards
  events:
    - http:
        path: api/v1/cards
        method: get
        cors: ${self:custom.corsConfig}
        authorizer:
          name: lambdaAuthorizer
          resultTtlInSeconds: 300
          identitySource: method.request.header.Cookie
          type: request

updateCard:
  handler: src/main/functions/cards/update-card.updateCard
  events:
    - http:
        path: api/v1/cards/{id}
        method: put
        cors: ${self:custom.corsConfig}
        authorizer:
          name: lambdaAuthorizer
          resultTtlInSeconds: 300
          identitySource: method.request.header.Cookie
          type: request

deleteCard:
  handler: src/main/functions/cards/delete-card.deleteCard
  events:
    - http:
        path: api/v1/cards/{id}
        method: delete
        cors: ${self:custom.corsConfig}
        authorizer:
          name: lambdaAuthorizer
          resultTtlInSeconds: 300
          identitySource: method.request.header.Cookie
          type: request

reactivateCard:
  handler: src/main/functions/cards/reactivate-card.reactivateCard
  events:
    - http:
        path: api/v1/cards/{id}/reactivate
        method: put
        cors: ${self:custom.corsConfig}
        authorizer:
          name: lambdaAuthorizer
          resultTtlInSeconds: 300
          identitySource: method.request.header.Cookie
          type: request

updateBill:
  handler: src/main/functions/bills/update-bill.updateBill
  events:
    - http:
        path: api/v1/bills/{id}
        method: put
        cors: ${self:custom.corsConfig}
        authorizer:
          name: lambdaAuthorizer
          resultTtlInSeconds: 300
          identitySource: method.request.header.Cookie
          type: request

deleteBill:
  handler: src/main/functions/bills/delete-bill.deleteBill
  events:
    - http:
        path: api/v1/bills/{id}
        method: delete
        cors: ${self:custom.corsConfig}
        authorizer:
          name: lambdaAuthorizer
          resultTtlInSeconds: 300
          identitySource: method.request.header.Cookie
          type: request

reactivateBill:
  handler: src/main/functions/bills/reactivate-bill.reactivateBill
  events:
    - http:
        path: api/v1/bills/{id}/reactivate
        method: put
        cors: ${self:custom.corsConfig}
        authorizer:
          name: lambdaAuthorizer
          resultTtlInSeconds: 300
          identitySource: method.request.header.Cookie
          type: request

createTransaction:
  handler: src/main/functions/transactions/create-transaction.createTransaction
  events:
    - http:
        path: api/v1/transactions
        method: post
        cors: ${self:custom.corsConfig}
        authorizer:
          name: lambdaAuthorizer
          resultTtlInSeconds: 300
          identitySource: method.request.header.Cookie
          type: request

getTransactions:
  handler: src/main/functions/transactions/get-transactions.getTransactions
  events:
    - http:
        path: api/v1/transactions
        method: get
        cors: ${self:custom.corsConfig}
        authorizer:
          name: lambdaAuthorizer
          resultTtlInSeconds: 300
          identitySource: method.request.header.Cookie
          type: request

deleteTransaction:
  handler: src/main/functions/transactions/delete-transaction.deleteTransaction
  events:
    - http:
        path: api/v1/transactions/{id}
        method: delete
        cors: ${self:custom.corsConfig}
        authorizer:
          name: lambdaAuthorizer
          resultTtlInSeconds: 300
          identitySource: method.request.header.Cookie
          type: request

markBillPaid:
  handler: src/main/functions/transactions/mark-bill-paid.markBillPaid
  events:
    - http:
        path: api/v1/transactions/{id}/pay
        method: put
        cors: ${self:custom.corsConfig}
        authorizer:
          name: lambdaAuthorizer
          resultTtlInSeconds: 300
          identitySource: method.request.header.Cookie
          type: request

createBill:
  handler: src/main/functions/bills/create-bill.createBill
  events:
    - http:
        path: api/v1/bills
        method: post
        cors: ${self:custom.corsConfig}
        authorizer:
          name: lambdaAuthorizer
          resultTtlInSeconds: 300
          identitySource: method.request.header.Cookie
          type: request

getBills:
  handler: src/main/functions/bills/get-bills.getBills
  events:
    - http:
        path: api/v1/bills
        method: get
        cors: ${self:custom.corsConfig}
        authorizer:
          name: lambdaAuthorizer
          resultTtlInSeconds: 300
          identitySource: method.request.header.Cookie
          type: request

getMonthlySummary:
  handler: src/main/functions/summary/get-monthly-summary.getMonthlySummary
  events:
    - http:
        path: api/v1/summary
        method: get
        cors: ${self:custom.corsConfig}
        authorizer:
          name: lambdaAuthorizer
          resultTtlInSeconds: 300
          identitySource: method.request.header.Cookie
          type: request

generateMonthlyBills:
  handler: src/main/functions/bills/generate-monthly-bills.generateMonthlyBills

defineAuthChallenge:
  handler: src/main/functions/auth/define-auth-challenge.handler

createAuthChallenge:
  handler: src/main/functions/auth/create-auth-challenge.handler

verifyAuthChallengeResponse:
  handler: src/main/functions/auth/verify-auth-challenge-response.handler
